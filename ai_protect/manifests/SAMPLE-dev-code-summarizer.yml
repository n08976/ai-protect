# Generated by the ai-protect UI manifest editor.
# Edit through /manifests/<name>/edit or update YAML directly — either is fine.

name: SAMPLE-dev-code-summarizer
owner: dev-tools@example.health
on_call: dev-tools-oncall@example.health
description: Single-user CLI that summarizes diffs in pull requests. Public/internal source code only;
  no PHI; advisory output.
data_sensitivity: confidential
decision_impact: advisory
integration_footprint: read_only
user_population: single_user
models:
- name: claude-haiku-gateway
  provider: anthropic
  model: claude-haiku-4-5-20251001
  via_gateway: true
  baa_covered: false
  auth_env: ANTHROPIC_API_KEY
surfaces:
  has_user_chat: true
expected_data_scopes:
- internal
source_paths:
- ./
