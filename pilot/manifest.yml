name: assure-pilot
owner: pilot@example.com
on_call: pilot@example.com
description: Actions pilot — exercises the assure.yml gate → auto-fix → verify → re-gate flow on a seeded insecure call.
data_sensitivity: public
decision_impact: advisory
integration_footprint: read_only
user_population: single_user
surfaces:
  has_user_chat: false
source_path: pilot
